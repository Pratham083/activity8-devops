## Implemented Endpoints
GET: default 'hello world' message
POST: echo the same body at '/post'
PUT: echo the same body at '/put'
DELETE: return a message 'Deleted'

## Implemented Tests
Tested all endpoints for their expected output.
Included all CI tests noted in the doc (test.yml, lint, codeql)
